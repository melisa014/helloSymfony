imports:
    - { resource: parameters.yml }
    - { resource: security.yml }
    - { resource: services.yml }

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices/configuration.html#application-related-configuration
parameters:
    locale: ru

framework:
    #esi: ~
    translator: { fallbacks: ['%locale%'] }
    secret: '%secret%'
    router:
        resource: '%kernel.project_dir%/app/config/routing.yml'
        strict_requirements: ~
    form: ~
    csrf_protection: true
    validation: { enable_annotations: true }
    #serializer: { enable_annotations: true }
    templating:
        engines: ['twig']
    default_locale: '%locale%'
    trusted_hosts: ~
    session:
        # https://symfony.com/doc/current/reference/configuration/framework.html#handler-id
        handler_id: session.handler.native_file
        save_path: '%kernel.project_dir%/var/sessions/%kernel.environment%'
    fragments: ~
    http_method_override: true
    assets: ~
    php_errors:
        log: true

# Twig Configuration
twig:
    debug: '%kernel.debug%'
    strict_variables: '%kernel.debug%'

# Doctrine Configuration
doctrine:
    dbal:
        connections:
            # Connection named pgsql
            pgsql:
                driver: pdo_pgsql
                host: '%database_host%'
                port: '%database_port%'
                dbname: '%database_name%'
                user: '%database_user%'
                password: '%database_password%'
                charset: UTF8
                # if using pdo_sqlite as your database driver:
                #   1. add the path in parameters.yml
                #     e.g. database_path: '%kernel.project_dir%/var/data/data.sqlite'
                #   2. Uncomment database_path in parameters.yml.dist
                #   3. Uncomment next line:
                #path: '%database_path%'

    orm:
        auto_generate_proxy_classes: '%kernel.debug%'
        naming_strategy: doctrine.orm.naming_strategy.underscore
        auto_mapping: true
        default_entity_manager: hello

# Doctrine-migrations Configuration
doctrine_migrations:
    dir_name: "%kernel.root_dir%/DoctrineMigrations"
    namespace: Application\Migrations
    table_name: migration_versions
    name: Application Migrations
    # Version >=1.2 Possible values are: "BY_YEAR", "BY_YEAR_AND_MONTH", false
    organize_migrations: false 
    
# Swiftmailer Configuration
swiftmailer:
    transport: '%mailer_transport%'
    host: '%mailer_host%'
    username: '%mailer_user%'
    password: '%mailer_password%'
    spool: { type: memory }

# FOSUserBundls Configuration
fos_user:
#    # other valid values are 'mongodb', 'couchdb' and 'propel'
    db_driver: orm 
    firewall_name: main
    user_class: AppBundle\Entity\User
    from_email:
        address: you@example.com
        sender_name: You
    registration:
        form:
            type: AppBundle\Form\RegistrationType
            validation_groups:  [Registration, Default]
#            template: 'app\Resources\user\register.html.twig'

#fos_user:
#    db_driver: orm
#    firewall_name: main
#    user_class: AppBundle\Entity\User
#    use_listener:           true
#    use_flash_notifications: true
#    use_authentication_listener: true
#    use_username_form_type: true
    # change it to the name of your entity/document manager if you don't want to use the default one.
#    model_manager_name:     null  
#    from_email:
#        address:        webmaster@example.com
#        sender_name:    webmaster
#    profile:
#        form:
#            # or 'fos_user_profile' on Symfony < 2.8
#            type:               Acme\UserBundle\Form\RegistrationType 
#            name:               fos_user_profile_form
#            validation_groups:  [Profile, Default]
#    change_password:
#        form:
#            # or 'fos_user_change_password' on Symfony < 2.8
#            type:               Acme\UserBundle\Form\RegistrationType 
#            name:               fos_user_change_password_form
#            validation_groups:  [ChangePassword, Default]
#    registration:
#        form:
             # or 'fos_user_registration' on Symfony < 2.8
#            type:               FOS\UserBundle\Form\Type\RegistrationFormType
#            type:               AppBundle\Form\RegistrationType
#            name:               fos_user_registration_form
#            validation_groups:  [Registration, Default]
#            template: 'app\Resources\user\register.html.twig'
#        confirmation:
            # Use this node only if you don't want the global email address for the confirmation email
#            from_email: 
#                address:        ...
#                sender_name:    ...
            # change to true for required email confirmation
#            enabled:    false 
#            template:   '@FOSUser/Registration/email.txt.twig'
#    resetting:
#        # Value in seconds, logic will use as hours
#        retry_ttl: 7200 
#        token_ttl: 86400
#        email:
#            # Use this node only if you don't want the global email address for the resetting email
#            from_email: 
#                address:        ...
#                sender_name:    ...
#            template:   '@FOSUser/Resetting/email.txt.twig'
#        form:
#            # or 'fos_user_resetting' on Symfony < 2.8
#            type:               FOS\UserBundle\Form\Type\ResettingFormType 
#            name:               fos_user_resetting_form
#            validation_groups:  [ResetPassword, Default]
#    service:
#        mailer:                 fos_user.mailer.default
#        email_canonicalizer:    fos_user.util.canonicalizer.default
#        username_canonicalizer: fos_user.util.canonicalizer.default
#        token_generator:        fos_user.util.token_generator.default
#        user_manager:           fos_user.user_manager.default
#    group:
#        group_class:    ~ # Required when using groups
#        group_manager:  fos_user.group_manager.default
#        form:
#            # or 'fos_user_group' on Symfony < 2.8
#            type:               FOS\UserBundle\Form\Type\GroupFormType 
#            name:               fos_user_group_form
#            validation_groups:  [Registration, Default]